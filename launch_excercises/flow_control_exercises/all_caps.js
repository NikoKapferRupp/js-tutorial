function upperCaseGreaterThanTen(str) {
    if (str.length > 10) {
        return str.toUpperCase();
    } else {
        return str;
    }
}

function numberRange(num) {
    if (num <= 50 && num >= 0) {
        console.log(num + ' is between 0 and 50')
    } else if (num < 0){
        console.log(num + 'is less than 0')
    } else if ()
}